# Diamond AI Platform

A lightweight Streamlit app for exploring diamond design parameters such as facet angle, surface quality, thickness, and cut depth.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```
